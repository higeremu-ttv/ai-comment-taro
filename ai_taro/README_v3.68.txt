■ v3.68 適用手順

1. このフォルダの6ファイルを ai_taro フォルダに上書きコピー
2. screen_module.py を【手動で削除】してください（今回から不要）
3. secrets.py に SCREEN_MONITOR_INDEX の行があれば削除（残っていても害はなし）

■ 変更内容
[修正1] 音声呼びかけがほぼ効かなかった問題を修正
  「太郎、○○して」「コメント太郎〜」で反応するように
  （旧: 「AIコメント太郎」と完全一致の文頭でしか反応しなかった）

[修正2] クールダウン中の隠れクラッシュを修正
  存在しないメソッド _is_search_trigger を呼んでいて
  クールダウン中の発言処理が毎回エラーで消えていた（検索機能削除時の残骸）

[修正3] 発言処理スレッドのエラーをログに出すように
  今まではエラーが起きても画面に何も出ず発言が消えていた

[削除] 画面認識機能（screen_module）を完全撤去
  関連する設定・UI・依存ライブラリ（mss, Pillow）も削除

[整理] _call_gemini_flash → _call_gemini_lite に改名（実態に合わせた）
