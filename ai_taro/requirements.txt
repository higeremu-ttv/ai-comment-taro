# Twitch AI Comment Bot v2.06 - 依存ライブラリ
# インストール方法: pip install -r requirements.txt

# Twitch IRC Bot
twitchio==2.10.0

# 音声認識（Google Web Speech API）
SpeechRecognition==3.12.0

# 音声キャプチャ（Windowsのマイク）
pyaudio==0.2.14

# Gemini API（コメント生成・画面認識）
google-generativeai>=0.8.0

# スクリーンショット取得（ゲーム画面認識用）
mss>=9.0.0

# 画像処理（スクリーンショットのリサイズ）
Pillow>=10.0.0

# HTTP通信
requests==2.32.3

# 数値計算
numpy==1.26.4

# 注意: Ollamaは不要になりました（Gemini APIを使用するため）
# 注意: faster-whisperは不要です（VRAMを使用しません）
# インターネット接続が必要です（Google Web Speech API + Gemini APIを使用するため）
